# R20 synthesis (2026-05-09)

> **Authoring note.** Claude wrote the Claude baseline response. Codex
> was silent for >60 minutes after the routing event, so Claude wrote a
> Codex stand-in response (clearly marked) per the operator resilience
> directive. This synthesis is therefore single-author. When Codex
> re-attaches and ratifies or amends the stand-in, this synthesis
> section is mutable — Codex MAY append a `## Codex amendment` block
> below without rewriting the full file.

- routing event: 2026-05-09T19:14:49Z (`decision/proposed/major`, task_id `r20-explosive-intelligence-growth-doping-2026-05-09`)
- prompt sha256: `34041159919ac1fb646c0a76d3ea3a9c3ecd0a2cf07f9dda87f88af924a2d5a5`
- Claude baseline: `iterations/codex_scout_tasks/r20_explosive_growth_response_claude_2026_05_09.md` (PR #173 merged 19:29:30Z)
- Codex stand-in: `iterations/codex_scout_tasks/r20_explosive_growth_response_codex_2026_05_09.md` (this PR)
- synthesis status: written by Claude solo; Codex amendment block reserved at file bottom

---

## Agreements (both responses)

1. **Phase D measurements are the verifiable axis-A baseline.** Nine merged R17/R18/R19 PRs with JSON evidence files on `main`. EVOLUTION_INDEX.md should backfill all nine as round-zero entries.
2. **Owner split matches the prompt's recommendation.** Claude leads R20.1 / R20.4 / R20.5; Codex leads R20.2 / R20.3 / R20.6. No reassignment.
3. **PR order**: R20.1 → R20.5 → R20.2 → R20.4 → R20.3 → R20.6.
4. **Profile S is non-negotiable**: zero internet, zero LLM, zero transitive import of LLM provider libraries. Subprocess-isolated test asserting empty `sys.modules` keys for `anthropic` / `openai` / `ollama`.
5. **Privacy redaction**: ON by default for any cloud-bound prompt; the bypass flag must log a warning and require auditability.
6. **A/B 20% quality threshold for R20.3** is a deployment gate, not a measurement gate. Below-threshold candidates are still recorded in EVOLUTION_INDEX.md.
7. **Recursive self-improvement substrate IS the bridge.** Don't invent a parallel one. `prompts/review/{architect,security,reliability}.md` are missing today (verified) — R20.5 creates them as part of process isolation.
8. **Minimum viable overnight scope floor**: R20.1 + R20.5 skeleton + R20.4 Profile S subprocess test. Other PRs are upside, not floor.

## Disagreements (none material)

- The Codex stand-in adds two flavor differences (cycle-time split into `claim_to_push` + `push_to_merge`; redactor patterns under `<AGENT_BRIDGE_RUNTIME_ROOT>/redactor_patterns.json`). Both are accepted by Claude. Neither blocks PR ordering.
- The Codex stand-in flags `gh pr` CLI as a coordination chokepoint. Claude agrees as informational but does not rate it actionable in R20 scope. Logged for R21.

---

## Final PR plan

| # | PR | Owner | Reviewer | Gate | Measurement plan |
|---|---|---|---|---|---|
| 1 | R20.1 EVOLUTION_INDEX.md | Claude | Codex | Schema accepted; 9 prior wins backfilled; CI green | Axis C cycle_time pinned for the 9 prior PRs; per-round delta visible |
| 2 | R20.5 R16 process isolation | Claude | Codex | `Invoke-WaggleReview.ps1 -Role architect/security/reliability` runs separate processes; synthesis pass present; tests; no Bash in review mode | Three independent process runs reproducible; each can fail independently |
| 3 | R20.2 BridgeLLMClient prototype | Codex | Claude | Interface + config loader + telemetry + cache placeholder + ollama provider + heuristic fallback + tests; no cloud creds required; Profile S still works | Telemetry log shape on disk; per-call budget enforced; fallback chain exercised in tests |
| 4 | R20.4 Profile S/M/L config | Claude | Codex | `solver-profiles/{small,medium,large}.json`; `Start-WaggleDanceSolver.ps1`; subprocess-isolated Profile S test; M degrades to heuristic if LLM missing; L redaction-on-by-default | Boot succeeds in each profile; Profile S test asserts no LLM imports; no secrets printed |
| 5 | R20.3 first doping point | Codex | Claude | Exactly one chosen point; A/B implementation; quality test; ≥20% quality gain (else Decision B); behind config flag | quality_score_control vs quality_score_treatment; latency p50/p95/p99 both arms; cost; fallback distribution |
| 6 | R20.6 release-readiness | Codex | Claude | Only after 1–5 status known. Real release if R20.3 added a runtime API; otherwise release-readiness Decision B doc. | semver bump correctness; CHANGELOG matches measurements; Docker image builds + smoke test if attempted |

### Minimum viable overnight scope (decision)

Floor: 1 + 2 + 4 (Profile S only). Defer M/L if time-tight. R20.3 ships only if quality oracle is real; else Decision B. R20.6 ships as release-readiness doc only.

### Bridge claim plan

Each PR opens with a bridge claim against `r20-<owner>-<short-task>-2026-05-09`. WriteScope is exactly the files touched. Claim is released on done/pushed.

### 5-hour checkpoint format

```
Type:    decision
Status:  reported
Severity:major
TaskId:  r20-explosive-intelligence-growth-doping-2026-05-09
Message: "R20 midpoint: R20.1=<status>; R20.2=<status>; R20.3=<status>;
          R20.4=<status>; R20.5=<status>; R20.6=<status>; blockers=<short>;
          metrics=<short>"
```

### Morning summary format

```
Type:    decision
Status:  reported
Severity:major
TaskId:  r20-explosive-intelligence-growth-doping-2026-05-09
Message: "R20 morning summary: (1) PR numbers + one-line description each;
          (2) all new before/after metrics; (3) current EVOLUTION_INDEX.md
          state; (4) top 3 next bottlenecks for R21."
```

Plus write `iterations/codex_scout_tasks/r20_morning_summary_2026_05_10.md` with the same content expanded.

---

## First PR start condition

R20.1 (EVOLUTION_INDEX.md) starts immediately after this synthesis lands on `main`. Claude takes the claim. The 9 prior PRs (#165, #166, #167, #168, #169, #170, #171, #172, #173) are backfilled with the schema agreed in Claude's baseline:

```yaml
- session_id: ...
  pr: ...
  owner: ...
  reviewer: ...
  axis_a_before_ms: ...
  axis_a_after_ms: ...
  axis_a_metric: ...
  axis_b_quality: null
  axis_c_claim_to_push_minutes: ...
  axis_c_push_to_merge_minutes: ...
  failed_attempts: ...
  pre_merge_findings_caught: ...
  post_merge_audit_findings: ...
  runtime_behavior_changed: bool
  lessons_learned: ...
  next_bottleneck: ...
```

The Codex stand-in's contributions (`pre_merge_findings_caught`, `post_merge_audit_findings`, `runtime_behavior_changed`, claim/merge time split) are folded into the schema.

---

## Codex amendment (reserved)

> When Codex re-attaches, append below this line. Do not rewrite the
> agreements/disagreements/PR plan sections without explicit operator
> approval.
