# v3.12.0 Security / Privacy Precheck - 2026-05-22

Status: preliminary local precheck only. This is not final stable evidence.

## Cloud / Privacy Tests

Command:

```powershell
python -m pytest tests/test_bridge_llm_redactor.py tests/test_bridge_llm_client.py tests/unit_core/test_bridge_llm_adapter.py --basetemp=.pytest_tmp_privacy_gate
```

Result:

```text
74 passed
```

Profile S smoke also passed:

```text
SMOKE_OK
```

## Bandit

Tool:

```text
bandit 1.9.4
```

Initial command:

```powershell
python -m bandit -r waggledance core -f json -o docs/runs/release_soak_evidence/v3.12.0_bandit_report.json
```

Initial result:

```text
HIGH: 0
MEDIUM: 32
LOW: 237
```

One real medium finding was fixed locally:

- `B506` in `waggledance/core/reasoning/solver_router.py`
- Change: replace `yaml.load(..., Loader=SafeLoader)` with `yaml.safe_load(...)`.
- Rationale: same safe YAML semantics, but uses the API Bandit recognizes.

Validation after fix:

```powershell
python -m pytest tests/unit_core/test_solver_signal_registry.py tests/autonomy/test_solver_router.py tests/autonomy_growth/test_solver_router_autonomy_consult.py --basetemp=.pytest_tmp_solver_router
```

```text
57 passed
```

Bandit after fix:

```powershell
python -m bandit -r waggledance core -f json -o docs/runs/release_soak_evidence/v3.12.0_bandit_report_after_solver_router_fix.json
```

```text
HIGH: 0
MEDIUM: 31
LOW: 237
```

Remaining MEDIUM categories:

```text
B104: 2
B307: 3
B310: 2
B608: 10
B614: 1
B615: 13
```

Additional B310 hardening after the first precheck:

- `waggledance/adapters/cli/start_runtime.py`: added http/https + netloc
  validation before the Ollama probe calls `urlopen`, with regression tests.
- `core/auto_install.py`: documented and suppressed B310 for the constant
  hard-coded HTTPS Voikko dictionary URL. This does not make the auto-download
  policy a release pass; it only closes Bandit's scheme warning.

Validation:

```text
pytest tests/test_ollama_probe.py tests/integration/test_runtime_cli.py tests/test_packaging_metadata.py --basetemp=.pytest_tmp_start_runtime2
=> 38 passed

python -m py_compile core/auto_install.py waggledance/adapters/cli/start_runtime.py waggledance/core/reasoning/solver_router.py tools/bridge_next_action.py
=> pass
```

Bandit after B310 hardening:

```text
HIGH: 0
MEDIUM: 29
LOW: 237
```

Remaining MEDIUM categories after B310 hardening:

```text
B104: 2
B307: 3
B608: 10
B614: 1
B615: 13
```

Additional static-security hardening after the no-stub release check:

- `B307` removed from product code by routing `constraint_engine` capsule
  expressions and `math_solver` arithmetic through AST-whitelisted
  `core.safe_eval`. The remaining internal `safe_eval` call is behind AST node
  and call-target validation.
- `B608` removed by using quoted SQLite identifiers for dynamic table/column
  names and by documenting allowlisted/parameterized SQL fragments.
- `B104` documented for the intentional Docker/production wildcard bind in
  `start_runtime.py`; behavior was not changed to localhost because that would
  break the production container entrypoint.
- `B614` removed by loading PyTorch checkpoints with `weights_only=True`.
- `B615` resolved by making Hugging Face model loads local-only and documenting
  the local-only residual suppressions. This prevents silent unpinned Hub
  downloads during release runs.

Validation:

```text
pytest tests/test_safe_eval.py tests/unit_core/test_math_solver.py tests/test_no_regressions.py --basetemp=.pytest_tmp_b307_core
=> 51 passed

python tests/test_constraint_engine.py
=> 11/11 passed

pytest tests/autonomy/test_capability_adapters.py tests/unit_core/test_memory_engine_orchestration.py --basetemp=.pytest_tmp_b307_adapters
=> 25 passed

pytest tests/unit_core/test_storage_health.py tests/unit_core/test_retention_policy.py --basetemp=.pytest_tmp_b608_storage
=> 26 passed

pytest tests/unit/test_sqlite_shared_memory.py tests/unit/test_sqlite_trust_store.py --basetemp=.pytest_tmp_b608_memory_trust
=> 39 passed

pytest tests/providers/test_provider_plane.py tests/autonomy_growth/test_dispatch_by_features.py --basetemp=.pytest_tmp_b608_provider_cp
=> 15 passed

pytest tests/integration/test_runtime_cli.py tests/autonomy/test_runtime_cutover_config.py tests/test_ollama_probe.py --basetemp=.pytest_tmp_b104_runtime
=> 84 passed

pytest tests/unit_core/test_memory_engine_orchestration.py tests/test_pipeline.py --basetemp=.pytest_tmp_hf_local
=> 16 passed

pytest tests/test_b4_error_handling.py tests/autonomy/test_capability_adapters.py --basetemp=.pytest_tmp_translation_local
=> 18 passed

python -m py_compile core/safe_eval.py core/math_solver.py core/constraint_engine.py core/micro_model.py core/opus_mt_adapter.py core/translation_proxy.py waggledance/adapters/cli/start_runtime.py
=> pass
```

Current Bandit report:

```powershell
python -m bandit -r core waggledance -f json -o docs/runs/release_soak_evidence/v3.12.0_bandit_report_after_static_hardening_zero_medium.json
```

```text
HIGH: 0
MEDIUM: 0
LOW: 237
```

No-stub R22.3 / Profile L environment check:

```text
ANTHROPIC_API_KEY=False
WAGGLE_BRIDGE_LLM_ENABLED=False
WAGGLE_FALLBACK_CHAIN=False
WAGGLE_BRIDGE_LLM_REDACTION=False
```

No real Profile L treatment was run because the required cloud credentials and
enablement flags are not present. Stubbed treatment would violate the operator
instruction for release decisions.

Release impact:

- Static Bandit medium findings are closed in this local precheck.
- `security_privacy_gate` must still remain non-pass because dependency audit
  is still blocking.
- Phase16F carry-forward exists, but the current scan has more LOC and a
  different medium set/count than the old stable evidence. The v3.12.0
  stable gate should not blindly reuse the Phase16F pass.

## pip-audit

Tool availability:

```text
pip-audit 2.10.0
```

Initial sandboxed run failed because pip-audit tried to create a cache under
`C:\Users\janik\AppData\Local\pip-audit`.

Second sandboxed run used a workspace cache and reached PyPI, but network
access was blocked by the sandbox.

Escalated run:

```powershell
python -m pip_audit --skip-editable --cache-dir .pip-audit-cache -f json -o docs/runs/release_soak_evidence/v3.12.0_pip_audit_report.json
```

Result:

```text
pip-audit CLI: Found 52 known vulnerabilities in 21 packages
parsed JSON: 21 dependency entries with vulnerabilities, 52 vuln entries
```

Packages with vulnerability entries:

```text
aiohttp, cryptography, deep-translator, diffusers, gitpython, idna, js2py,
lxml, nltk, paramiko, pillow, pip, pyarrow, pygments, pypdf, pytest,
python-dotenv, requests, streamlit, ujson, urllib3
```

Direct CI/lower-bound slice:

- Updated lower bounds in `requirements.txt`, `requirements-ci.txt`, and
  `pyproject.toml`:
  - `aiohttp>=3.13.4`
  - `pytest>=9.0.3`
- Escalated real install succeeded; imported versions:
  - `aiohttp=3.13.5`
  - `pytest=9.0.3`
- Validation after install:

```text
pytest tests/test_release_gate_soak_evidence.py tests/tools/test_collect_soak_evidence.py tests/integration/test_runtime_cli.py tests/test_ollama_probe.py --basetemp=.pytest_tmp_dependency_ci_slice
=> 40 passed

pytest tests/test_bridge_llm_redactor.py tests/test_bridge_llm_client.py tests/unit_core/test_bridge_llm_adapter.py --basetemp=.pytest_tmp_dependency_privacy
=> 74 passed
```

Escalated pip-audit after direct CI deps:

```text
docs/runs/release_soak_evidence/v3.12.0_pip_audit_report_after_direct_ci_deps.json
```

```text
vulnerable dependency entries: 19
vulnerability entries: 41
```

This removed `aiohttp` and `pytest` from the installed-environment vulnerability
set. It does not fix Docker/reproducible release by itself because
`requirements.lock.txt` still pins the old versions and must be refreshed.

Fixable dependency slice after the direct CI/lower-bound update:

- Escalated real install upgraded the fixable vulnerable package set in the
  current venv overlay:
  `cryptography=46.0.7`, `diffusers=0.38.0`, `GitPython=3.1.50`,
  `idna=3.15`, `lxml=6.1.0`, `nltk=3.9.4`, `pillow=12.2.0`,
  `pip=26.1`, `pyarrow=23.0.1`, `Pygments=2.20.0`, `pypdf=6.10.2`,
  `python-dotenv=1.2.2`, `requests=2.33.0`, `streamlit=1.54.0`,
  `ujson=5.12.1`, and `urllib3=2.7.0`.
- Validation after install:

```text
pytest tests/test_release_gate_soak_evidence.py tests/tools/test_collect_soak_evidence.py tests/test_bridge_llm_redactor.py tests/test_bridge_llm_client.py tests/unit_core/test_bridge_llm_adapter.py --basetemp=.pytest_tmp_dependency_fixable_slice
=> 86 passed
```

- Installed-environment pip-audit after the fixable slice:

```text
docs/runs/release_soak_evidence/v3.12.0_pip_audit_report_after_fixable_deps.json
```

```text
vulnerable dependency entries: 3
vulnerability entries: 3
remaining no-fix packages: deep-translator 1.11.4, js2py 0.74, paramiko 4.0.0
skipped dependencies: torch, torchaudio, torchvision
```

- `pip check` still fails in the current inherited environment:

```text
moviepy 2.2.1 requires pillow<12.0,>=9.2.0, but pillow 12.2.0 is installed
xformers 0.0.35 requires torch>=2.10, but torch 2.7.1+cu118 is installed
```

Dependency lock remediation status:

- The lockfile remediation and Torch-family resolution are not included in this
  PR. They require a separate operator decision because the auditable
  `torch==2.11.0` path tested locally resolved to CPU wheels and would drop the
  current Windows `+cu118` CUDA build from the shipped lock.
- Therefore this PR does not claim a dependency-audit release pass and does not
  change `requirements.lock.txt`.
- `security_privacy_gate` must remain non-pass until the dependency-lock path is
  resolved in a dedicated PR with explicit CUDA/GPU tradeoff approval or a
  CUDA-preserving audited alternative.

Release impact:

- Static Bandit HIGH/MEDIUM findings are closed in this local precheck.
- Privacy/Profile S smoke passed locally.
- Dependency audit remains a separate release blocker, so the stable release
  evidence must remain HOLD.
