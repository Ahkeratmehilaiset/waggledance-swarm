# v3.12.0 Security / Privacy Receipt — 2026-09-10

Status: completed scoped host privacy validation, adopted as pre-source-subject
evidence. This receipt does not authorize stable publication, deployment,
signatures, or replacement of the release gate's other checks.

## Source and test result

The lead tested composition tree `bf7a9b435694dc944561adaef7b38c165d367d13`,
then committed that unchanged tree as
`1a6598c772a2c44dc225865cf545f93933bcb22c`. It combines PR #1673 and
PR #1674 with the preserved safe-YAML and privacy-collector fixes.

Command, run from the persistent composition worktree on Windows:

```powershell
python.exe -B -m pytest -q -ra -p no:cacheprovider -p pytest_asyncio.plugin --basetemp=C:/Python/project2/.codex-audit/composition-privacy-async-0910 --junitxml=.codex-audit/release-composition-20260910/privacy-async.xml tests/test_bridge_llm_redactor.py tests/test_bridge_llm_client.py tests/unit_core/test_bridge_llm_adapter.py
```

The interpreter was the existing dependency-validation virtual environment at
`C:/Python/waggledance-agent-worktrees/codex-lead-1-codex-lead-1-release-deps-20260910/.codex-audit/release-integration-20260910/root-validation/venv/Scripts/python.exe`.
Plugin autoload was disabled, with the already-installed asyncio plugin loaded
explicitly. No package installation or runtime configuration change was made.

Result at 2026-09-10T19:21:11Z, duration 3.42 seconds, exit 0, zero skips:

```text
87 passed
```

JUnit SHA-256:
`D9FF7BD4F8089032B7641C666AB0B8D937ED5ED83F559E1A584A329FE73F658D`.
An earlier invocation omitted the asyncio plugin and recorded 78 passes and
9 unsupported-async failures. Those results remain in the local audit
`privacy.xml`; the corrected command above changed no source code.

## Profile S host smoke

The same interpreter executed the tracked smoke assertions against that tree:

```powershell
python.exe -B -c "import sys; from waggledance.core.bridge_llm import BridgeLLMClient, BridgeLLMRedactor; leaked=[n for n in ('anthropic','openai','ollama') if n in sys.modules]; assert not leaked, leaked; assert not BridgeLLMClient.disabled('profile_s').is_enabled(); assert BridgeLLMRedactor().redact('alice@example.org').applied; print('SMOKE_OK')"
```

Result, exit 0:

```text
SMOKE_OK
```

This proves the stated imports, disabled-client behavior and email redaction
on the host. It is not a container smoke or a deployed runtime soak.
Fable independently obtained 87 passes and the same host smoke at predecessor
`459d7bf8483e6cc5b4a72ebe9cdccac3d272b016`; the lead rerun above tests the
actual combined tree rather than relying only on that predecessor result.

## Separate security evidence

- OSV QueryBatch audited all 113 exact lock pins at
  `459d7bf8483e6cc5b4a72ebe9cdccac3d272b016`: zero known vulnerabilities.
  The lock is byte-identical in the tested composition. Raw report:
  `v3.12.0_pip_audit_report_lock_after_prune_osv.json`, SHA-256
  `90E51E5D4582C9EC2949A63CC5D66911C8158DA9B269BF9EBB31986494435D5D`.
- Fable ran Bandit 1.9.4 at
  `fdbd0c3d9a0b44e0a287b0e2aff0d4bac98a9eff` over 614 files in `waggledance`
  and `core`: HIGH 0, MEDIUM 0, LOW 232, errors 0. Exit 1 reflects the
  remaining LOW findings; those findings are not closed. The scanned subtrees
  are unchanged in the tested composition. Raw report:
  `v3.12.0_bandit_report_after_static_hardening_zero_medium.json`, SHA-256
  `2B3092074814DDC8DCB011F35DA4CF4DB0247C5030104EDF8FC3EFADC405A2F7`.

Raw reports retain their original contents and timestamps. These scoped
checks do not establish absence of all vulnerabilities or completion of
source-bound CI, Axis proofs, Docker policy, operator decisions or elapsed
runtime soak. Those remain independent release requirements.

This receipt must live inside the eventual source subject S. If the attested
source changes, revalidate it before choosing a new S; do not alter this
non-carrier document in an evidence-only commit after S.
