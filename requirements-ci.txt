# CI dependencies — cross-platform subset of pyproject.toml [project.dependencies]
# Used by .github/workflows/tests.yml on Ubuntu runners.
# For local development use: pip install -r requirements.lock.txt

# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
aiosqlite>=0.20.0
websockets>=13.0
pyyaml>=6.0
psutil>=5.9.0
# chromadb intentionally absent (lead guidance 2026-08-26): the stable
# Docker/CI input must omit it. Chroma-dependent tests import lazily and
# skip when the package is missing; opt-in only via the [chroma] extra.
transformers>=4.40.0
torch>=2.13.0
sentencepiece>=0.1.99
duckduckgo-search>=6.0
aiohttp>=3.13.4
feedparser>=6.0.11
anthropic>=0.30.0
paho-mqtt>=2.0.0
networkx>=3.0
pydantic>=2.0
scikit-learn>=1.4.0
joblib>=1.3.0
prometheus-client>=0.20.0

# Test
pytest>=9.0.3
pytest-asyncio>=1.3.0
# jsonschema was previously satisfied only as a chromadb transitive; the
# schema contract tests (tests/contracts/) import it directly, so it must
# be declared explicitly now that chromadb is de-scoped (2026-08-26).
jsonschema>=4.25.0

# Used by audio/stats tests
scipy>=1.12.0
