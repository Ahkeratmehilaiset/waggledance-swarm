# v3.12.0 — Release Notes (candidate)

* Release target: **v3.12.0** stable.
* No earlier than: **2026-05-24** (per R22.5 soak / promotion gate;
  see `docs/release/RELEASE_READINESS.md` and `tools/check_release_gate.py`).
* Posture: prepare-now-candidate, do not publish stable until the
  soak window completes and operator confirms the D1 PII-history
  decision.
* Previous stable: `v3.8.0` (2026-05-04). v3.9–v3.11 remain prereleases.
* Docker registry: GHCR primary; no Docker Hub mirror yet.

This document is the candidate release surface. It is generated from the
landed substrate + impl PRs through 2026-05-12. Numbers and PR references
are measured / observed, not aspirational.

## Highlights

* **Audit-fix series complete** — coordinated multi-agent (Claude Code +
  OpenAI Codex) parallel skeptic audit produced 88 findings; the
  operator-approved D1/D2/D3 decision wireups + 28 H-numbered fixes
  landed across PRs #237–#277.
* **EIG2 M0 + M1.0 + substrate 55/55** — the entire 50-leaps menu plus
  L51–L54 plus L60-NEW plus the strategic capstone (ADR-062) are pinned
  in `main` as machine-checkable ADR + JSON contract + contract test
  trios. 55 ADRs (ADR-010 through ADR-062) carry the architectural
  intent forward without prematurely landing runtime code.
* **L54-reframed: 99.6 % cold-boot reduction** — `bind_executors()`
  rewritten from 23 eager `try/except` adapter constructors to a
  lazy-factory registry (PR #340). Bench: 6855 ms median → 27 ms
  median across the capability loader. `capability_loader.py` shrank
  488 → 145 lines (-70 %). First `get_executor()` call triggers
  `importlib.import_module` + adapter `__init__`; everything else is
  pure registration.
* **Release gate / soak evidence guardrail** — `tools/check_release_gate.py`
  (PR #292) now fail-closes any stable promotion that lacks
  `waggledance.release_soak.v1` evidence covering CI / Profile S /
  security / Axis / Docker / anti-claim fields. 336-hour soak window
  is enforced before stable can ship.
* **AI-Assisted Bootstrap Kit (strategic capstone, ADR-062)** —
  contract + first GADGET-profile kit content seeded
  (`configs/bootstrap_kit/GADGET.yaml`, signature-hash-anchored,
  pending operator sign-off per BSK-007).
* **Magma EventLog perf-guard threshold** — relaxed 50 → 500 ms
  (PR #341) to accommodate GHA hosted-runner variance while still
  catching genuine O(N) regressions; unblocked substrate landings
  #318 and #326.

## Operator-decision wireups (D2 + D3 chains)

Carried over from prior CHANGELOG entries because they form the
foundation v3.12.0 stands on:

* **D2.1 — `container.control_plane_db` lifecycle (#253)** — the
  ControlPlaneDB was instantiated only by offline tools, so the R25
  decision histogram returned `insufficient-data`. Now warmed by
  api.py lifespan, closed on shutdown.
* **D2.2 — `RuntimeGapDetector` emits on low-confidence chat (#260)** —
  responses below confidence threshold now write `runtime_gap_signal`
  rows.
* **D2.3 — `autogrowth_scheduler` background ticker (#261)** — closes
  the autonomy-growth loop: gap signals → mined-solver candidates →
  auto-promotion. Profile S still disables the lane.
* **D3.2 — `AgentLifecycleManager` wired into Container (#251)** —
  fixes the pre-H12 multi-profile bug where
  `profiles=[APIARY, HOME]` agents were dropped.
* **D3.3 Phase A — `YAMLBridge.build_system_prompt` w/ f-string
  fallback (#252)** — orchestrator prompts route through YAMLBridge's
  ASSUMPTIONS / DECISION_METRICS / locale-overlay assembly.
* **D3.4 — `BridgeLLMClient` 4-tier chain wired into `container.llm`
  (#257)** — production LLM calls now route via
  cache → local-ollama → cloud → heuristic with
  `BridgeLLMRedactor` active on cloud calls.

## EIG2 M0 + M1.0 surface in `main`

The 55 ADRs pinned during the 2026-05-11 ↔ 2026-05-12 substrate sprint:

* **ADR-010 … 020** — M0 preflight set: scope freeze, RCO inventory,
  queue+backpressure, atomic flip, no-human policy, bridge schema.
* **ADR-021** — progressive replay L0–L4 contract (M1.0 anchor).
* **ADR-022 … 030** — provenance + cache + verification stack
  (forensic snapshot rotation, tip cache, predictive L1 prefetch,
  risk-tiered L3 budget, Merkle hash verification, cold-tier
  read-through, zstd-at-rest, compact decision card, delta supersedes).
* **ADR-031 … 034** — failure-class learning loop (confidence-bin gap
  mining, cross-agent failed-candidate broadcast, failure-pattern
  mining, anti-cargo-cult check).
* **ADR-035 … 040** — trust + tunnel substrate (stability_score,
  latency_consistency, temporal trust decay, tunnel overlay,
  multi-cell portfolio, negative tunnels).
* **ADR-041** — capability factory lazy binding (paired with the
  L54-reframed implementation PR #340).
* **ADR-042 … 048** — routing + telemetry substrate
  (co-occurrence mining, curiosity-gradient routing, temporal layers,
  trust-staged routing, color-class interleaving, cell-pair telemetry,
  solver-portfolio promotion).
* **ADR-049 … 054** — autonomy + retirement substrate
  (sleep-time consolidation, domain bridging, solver retirement,
  multi-objective promotion, operator-feedback amplifier,
  queue+backpressure compact card).
* **ADR-055 … 060** — profile-aware substrate (profile-aware budgets,
  per-profile GC tuning, LRU memoization pure hot path,
  cross-validation score, domain-specific trust vector,
  Bayesian trust update).
* **ADR-061** — god-class decomposition (L52, closes 55/55 gap).
* **ADR-062** — **AI-Assisted Bootstrap Kit** strategic capstone.
  Differentiates WaggleDance from peer multi-agent frameworks
  (Aider/Cline code-only, AutoGen/CrewAI orchestration-only) as the
  first to use external AI reasoning models (Claude Opus, OpenAI Codex)
  as a kit-generation source for fresh-deploy bootstrap state.

## Audit findings landed (28 H-numbered, from PRs #237–#277)

See the prior CHANGELOG entry (2026-05-11) for the full table of
H1, H4, H11, H12, H14, H17, H21–H23, H24, H26–H28, H30, H32–H35a,
H38, H42–H49, H51–H54, H58. Notable highlights:

* **H1 + H24 + H26 + H27 + H28 (#245)** — AliasRegistry-derived
  agent.domain + hex_cells selector extension. Hub-dominance
  100 % → ~1 %.
* **H47 (#242)** — batched trust fetch + async `case_store.save_case`.
  ~37 ms per chat collapsed to one DB call on HOME profile (75 agents).
* **H21 (#237)** — `yaml.CSafeLoader` for agent + axiom load
  (~7× boot speedup).
* **H49 + D3.4 (#257)** — full 4-tier BridgeLLMClient wiring.
* **H54 (#240)** — Finnish HETU / IBAN / Y-tunnus PII redaction
  ordered before phone regex.

## Post-audit RCO hardening closure (2026-05-12)

* **EIG2 M0 / M1 documentation and architecture checkpoints**
  (#263, #269, #270) — records the M0 reality-check inventory, ADR
  set, and M1 architecture map.
* **BridgeLLM + provider parameter preservation (#272, B32)** —
  generation parameters now flow through the request path instead
  of being silently dropped by provider defaults.
* **Finnish PII redactor expansion (#271, B35/B37)** — covers
  expanded HETU separators and lowercase IBAN.

## Substrate sprint (2026-05-12)

Land sequence (Claude lane + Codex lane parallel, autonomous):

* **PR #281** — PEP 562 lazy package init pattern reference.
* **PR #283** — ADR-010 … 020 set.
* **PR #284** — L51 strict-tuple contract semantics.
* **PR #285** — ADR-021 progressive replay L0–L4 contract.
* **PR #286** — 3 spot-finding fixes (after 56 % FP rate
  re-assessment of the spot-9 audit batch).
* **PR #287 … #338** — substrate ADRs (Claude lane, 39 ADRs).
* **PR #340** — L54-reframed lazy capability binding implementation.
* **PR #341** — magma EventLog O(N) guard threshold 50 → 500 ms
  (test-infrastructure unblock for #318 / #326).
* **PR #315** — index/contracts guardian (Codex lane, kept in lockstep
  with each ADR landing; final rebase pending #318 / #326 merge).

## Tooling additions

* **`tools/check_release_gate.py`** — release promotion gate. Parses
  `docs/release/RELEASE_READINESS.md`, requires
  `waggledance.release_soak.v1` evidence, fail-closes until soak
  window completes + CI / Profile S / security / Axis / Docker /
  anti-claim fields pass.
* **`tools/sim_orchestrator.py`** — read-only replay harness for
  bridge events. Computes lane balance, handshake coverage,
  independent-convergence detection, RCO coverage, finding-quality
  metrics, and bridge_classify regression-class distribution. Used
  in the 2026-05-12 orchestrator design retrospective.
* **`tools/gen_gadget_bootstrap_kit.py`** — deterministic generator
  for `configs/bootstrap_kit/GADGET.yaml` with sha256-anchored
  signature_hash per ADR-062 BSK-004. First profile kit to ship.

## Operator action required before stable enable

1. **D1 PII-history scrub decision**. The release gate notes a
   blocked operator-only item: rewriting git history to remove
   pre-redaction PII. This requires force-push to `main` and is the
   operator's call, not Claude/Codex'.
2. **Bootstrap-kit sign-off**. `configs/bootstrap_kit/GADGET.yaml`
   carries `signed_off_by: pending_operator_review_v3.12.0`. Per
   BSK-007 the BootstrapKitLoader will reject this kit until the
   operator replaces that field with an explicit signer id +
   ISO8601 timestamp. (BootstrapKitLoader implementation is the
   Codex lane; it lands separately during the soak window.)
3. **Soak window completion**. 336 h running from 2026-05-10
   00:00 UTC, completes ≥ 2026-05-24 00:00 UTC. The gate enforces
   this; nothing to do but wait and watch.

## Truth statements (BUSL / claim discipline)

This release:

* Does **not** claim AGI, consciousness, model superiority, or
  any threshold-of-intelligence benchmark.
* Reports cold-boot reduction as a measured number on the specific
  `bind_executors()` call path (6855 ms → 27 ms median), not a
  whole-system or "intelligence" metric.
* Confirms the 55-ADR substrate is **substrate**, not runtime: the
  contracts gate the schema, the implementations land one-by-one
  through future PRs (L54-reframed is the only impl that landed
  inside this release; the other 54 ADRs remain as pinned design
  intent until their implementation PRs are written, reviewed, and
  merged).
* States Docker `:latest` will remain `v3.8.0` until the explicit
  stable promotion at 2026-05-24 or later.

## License and BUSL

License unchanged: BUSL-1.1 with Additional Use Grant.
Change Date harmonized to 2030-03-19 across `LICENSE`, `CLAUDE.md`,
Phase F markers, and `phase8.5` siblings.

## Co-authors

* **Claude Code** (Anthropic) — Opus 4.7 (1 M context). ADR
  substrate authorship, bootstrap-kit content generation,
  orchestrator simulation harness, magma test threshold relax,
  CHANGELOG + release-notes drafting.
* **OpenAI Codex** — index/contracts guardian, release-gate
  guardrail authoring (`tools/check_release_gate.py`),
  BootstrapKitLoader + BootstrapKitGenerator (ADR-062
  implementation lane).
* **Operator** — jani.korpi@gmail.com. Decision authority on D1,
  bootstrap-kit sign-off, soak window observation, stable
  promotion trigger.
